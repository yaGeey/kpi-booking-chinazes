# Тестування Booking Room

## Огляд

Цей проект використовує **Jest** та **React Testing Library** для тестування компонентів та утилітарних функцій.

## Структура тестів

```
booking/
├── components/
│   └── __tests__/
│       ├── Header.test.tsx
│       ├── Footer.test.tsx
│       ├── RoomCard.test.tsx
│       └── BookingForm.test.tsx
├── lib/
│   └── __tests__/
│       └── utils.test.ts
├── jest.config.ts
└── jest.setup.ts
```

## Команди для запуску тестів

```bash
# Запустити всі тести
pnpm test

# Запустити тести в режимі watch (автоматичне перезапускання)
pnpm test:watch

# Запустити тести з покриттям коду
pnpm test:coverage
```

## Покриття тестами

### Утилітарні функції (lib/utils.ts)

-  ✅ `formatDate()` - форматування дати в український формат
-  ✅ `formatPrice()` - форматування ціни з символом гривні
-  ✅ `calculateDays()` - підрахунок днів між датами
-  ✅ `calculateTotalPrice()` - обчислення загальної вартості

### Компоненти

-  ✅ **Header** - навігація, логотип, посилання
-  ✅ **Footer** - контакти, години роботи, copyright
-  ✅ **RoomCard** - відображення кімнати, ціна, зручності
-  ✅ **BookingForm** - валідація форми, обчислення вартості, відправка

## Що тестується

### Функціональне тестування

-  **Рендеринг компонентів**: Чи відображаються всі необхідні елементи
-  **Взаємодія користувача**: Кліки, введення тексту, відправка форм
-  **Валідація**: Перевірка правильності введених даних
-  **Розрахунки**: Обчислення ціни, днів, загальної вартості

### Інтеграційне тестування

-  **Форми бронювання**: Повний цикл заповнення та відправки
-  **Валідація дат**: Неможливість вибору минулих дат
-  **Розрахунок вартості**: Автоматичний підрахунок при зміні дат

## Конфігурація

### jest.config.ts

-  **testEnvironment**: jsdom (для тестування React компонентів)
-  **setupFilesAfterEnv**: jest.setup.ts (глобальні налаштування)
-  **moduleNameMapper**: підтримка @ alias для імпортів
-  **coverageProvider**: v8 (швидше покриття коду)

### jest.setup.ts

-  **@testing-library/jest-dom**: додаткові matchers для DOM тестування
-  **Mock next/navigation**: заглушка для Next.js роутера
-  **Mock next/image**: заглушка для Next.js Image компонента

## Приклади тестів

### Тест утилітарної функції

```typescript
it('should format price with hryvnia symbol', () => {
   const formatted = formatPrice(1500)
   expect(formatted).toBe('1 500 ₴')
})
```

### Тест компонента

```typescript
it('should render room name', () => {
   render(<RoomCard room={mockRoom} />)
   const roomName = screen.getByText('Люкс')
   expect(roomName).toBeInTheDocument()
})
```

### Тест з взаємодією користувача

```typescript
it('should update form fields on input', async () => {
   const user = userEvent.setup()
   render(<BookingForm roomId="1" roomName="Люкс" pricePerDay={2500} />)

   const nameInput = screen.getByLabelText(/ім'я/i)
   await user.type(nameInput, 'Іван Петренко')
   expect(nameInput).toHaveValue('Іван Петренко')
})
```

## Найкращі практики

1. **Використовуйте React Testing Library**: Тестуйте компоненти з точки зору користувача
2. **Ізолюйте тести**: Кожен тест повинен бути незалежним
3. **Використовуйте моки**: Для зовнішніх залежностей (API, роутер)
4. **Тестуйте поведінку, а не реалізацію**: Фокус на те, що робить компонент
5. **Використовуйте значущі назви тестів**: `it('should do something')`

## Метрики покриття

Після запуску `pnpm test:coverage` ви побачите:

-  **Statements**: Відсоток виконаних інструкцій
-  **Branches**: Відсоток перевірених умов (if/else)
-  **Functions**: Відсоток викликаних функцій
-  **Lines**: Відсоток виконаних рядків коду

Мета: **>80% покриття** для всіх категорій

## Наступні кроки

1. ✅ Встановити залежності: `pnpm install`
2. ✅ Запустити тести: `pnpm test`
3. 📝 Додати тести для інших компонентів:
   -  BookingList
   -  RoomFilters
   -  ImageGallery
4. 📝 Додати інтеграційні тести для сторінок
5. 📝 Додати E2E тести з Playwright

## Корисні ресурси

-  [Jest Documentation](https://jestjs.io/)
-  [React Testing Library](https://testing-library.com/react)
-  [Testing Library Best Practices](https://kentcdodds.com/blog/common-mistakes-with-react-testing-library)
-  [Next.js Testing](https://nextjs.org/docs/testing)
